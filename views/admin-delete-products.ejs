<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Delete Products</title>
<style>
body { font-family: Arial, sans-serif; padding: 30px; background: #f5f5f5; }
.container { max-width: 900px; margin: auto; }
table { width: 100%; border-collapse: collapse; background: #fff; box-shadow: 0 5px 15px rgba(0,0,0,0.1); border-radius: 10px; overflow: hidden; }
th, td { padding: 12px 15px; text-align: left; border-bottom: 1px solid #ddd; }
th { background-color: #0077b6; color: #fff; }
tr:hover { background-color: #f1f1f1; }
.btn { padding: 8px 12px; background-color: #e63946; color: #fff; border: none; border-radius: 6px; cursor: pointer; }
.btn:hover { background-color: #c1121f; }
</style>
</head>
<body>

<h2>Delete Products</h2>

<form action="/admin/delete-products" method="POST">
    <table>
        <thead>
            <tr>
                <th>Select</th>
                <th>Name</th>
                <th>Category</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            <% products.forEach(product => { %>
            <tr>
                <td><input type="checkbox" name="products" value="<%= product._id %>"></td>
                <td><%= product.name %></td>
                <td><%= product.category %></td>
                <td>₹<%= product.price %></td>
            </tr>
            <% }) %>
        </tbody>
    </table>
    <br>
    <button type="submit" class="btn" onclick="return confirm('Are you sure you want to delete selected products?')">Delete Selected</button>
</form>

<br>
<a href="/admin/dashboard">Back to Dashboard</a>

</body>
</html>