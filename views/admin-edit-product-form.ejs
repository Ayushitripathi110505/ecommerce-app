<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Edit Product</title>
<style>
body { font-family: Arial; padding: 30px; background: #f5f5f5; }
.container { max-width: 600px; margin: auto; background: #fff; padding: 20px; border-radius: 12px; box-shadow: 0 5px 15px rgba(0,0,0,0.2); }
input, textarea { width: 100%; padding: 8px; margin: 10px 0; border-radius: 6px; border: 1px solid #ccc; }
.btn { padding: 10px 15px; background-color: #0077b6; color: #fff; border: none; border-radius: 8px; cursor: pointer; }
.btn:hover { background-color: #023e8a; }
</style>
</head>
<body>

<div class="container">
<h2>Edit Product</h2>
<form action="/admin/edit-product/<%= product._id %>" method="POST">
    <label>Name:</label>
    <input type="text" name="name" value="<%= product.name %>" required>

    <label>Category:</label>
    <input type="text" name="category" value="<%= product.category %>" required>

    <label>Price:</label>
    <input type="number" name="price" value="<%= product.price %>" required>

    <label>Stock:</label>
    <input type="number" name="stock" value="<%= product.stock %>" required>

    <label>Description:</label>
    <textarea name="description" rows="5" required><%= product.description %></textarea>

    <label>Image URL:</label>
    <input type="text" name="image" value="<%= product.image %>">

    <button type="submit" class="btn">Save Changes</button>
</form>

<br>
<a href="/admin/edit-products">Back</a>
</div>

</body>
</html>